{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mx-auto mt-8 flex  flex-col justify-center items-center">
        <div class="max-w-lg">
        <h1 class="text-4xl font-bold mb-4">Welcome to my site!</h1>
        <img src="{{ asset('images/homePhoto.jpg') }}" alt="homePhoto" class="mb-4 rounded">

        <p class="text-lg mb-2">Click below to go to the login page:</p>
        <a href="{{ path('app_login') }}" class="bg-blue-500 text-white py-2 px-4 rounded inline-block mt-4">Go to Login</a>
    </div>
</div>
{% endblock %}
